{
  "name": "aetheris",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Aetheris — a cinematic 3D city builder you can play in the browser.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "vitest run",
    "smoke": "node scripts/smoke-browser.mjs",
    "unreal:check": "node scripts/check-unreal.mjs",
    "audio": "python3 scripts/generate-audio.py",
    "ue-assets": "python3 scripts/generate-ue-assets.py"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "puppeteer-core": "^25.7.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^3.0.4"
  }
}
